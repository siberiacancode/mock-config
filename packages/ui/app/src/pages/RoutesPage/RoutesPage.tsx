import { Typography } from '@/components';

export const RoutesPage = () => (
  <div className='flex h-full flex-col items-center justify-center gap-m text-center'>
    <Typography variant='h1'>Routes</Typography>
    <Typography className='text-foreground-secondary'>
      Searchable list of all routes is coming in the next iteration
    </Typography>
  </div>
);
